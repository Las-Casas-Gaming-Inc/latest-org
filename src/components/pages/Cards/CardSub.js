import React from "react";

function CardSub({ title }) {
  return (
    <div className="card-sub">
      <div className="card-content-sub">
        <h2 className="card-title-sub">{title}</h2>
      </div>
    </div>
  );
}

export default CardSub;
